name: mast_contributor_tools
channels:
  - conda-forge
  - anaconda
  - nodefaults
dependencies:
  - python=3.11
  - jupyter
  - pip
  - pip:
    - setuptools
    - setuptools_scm
    - wheel
    - click   
    - deepmerge
    - pyyaml
    - pre-commit
    - ruff
    - pytest
    - pytest-doctestplus
    - pytest-mock
    - pytest-cov
